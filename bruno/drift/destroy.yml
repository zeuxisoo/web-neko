info:
  name: destroy
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/drift/destroy/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
