info:
  name: update-detail
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/account/profile/update-detail"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "description": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
