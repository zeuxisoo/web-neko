info:
  name: destroy
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/pulse/bookmark/destroy/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
