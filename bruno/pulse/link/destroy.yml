info:
  name: destroy
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/pulse/link/destroy/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
