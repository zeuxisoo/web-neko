info:
  name: unsaved
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/pulse/link/unsaved"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
