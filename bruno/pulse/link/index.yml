info:
  name: index
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/pulse/link/index?keyword=.org"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: keyword
      value: .org
      type: query
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
