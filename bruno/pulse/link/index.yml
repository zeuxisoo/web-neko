info:
  name: index
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/pulse/link/index"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
