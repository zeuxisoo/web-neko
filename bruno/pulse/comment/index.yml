info:
  name: index
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/pulse/comment/index/13"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
