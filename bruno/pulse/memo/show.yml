info:
  name: show
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/pulse/memo/show/4"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
