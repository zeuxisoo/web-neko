info:
  name: destroy
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/pulse/memo/destroy/3"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
